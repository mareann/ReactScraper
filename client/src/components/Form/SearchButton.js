import React from "react";
//import "./FormComponents.css";

const SearchButton = (props) => {
	console.log("button")
return(
    <button type="submit" className={"btn btn-primary fontStyle"} id={"searchBtn"} onClick={this.handleClick}>Search</button>
)
}
export default SearchButton;